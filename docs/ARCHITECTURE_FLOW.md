# 🏗️ Complete Architecture Flow - Kafka & WebSocket

## Overview

Your e-commerce platform uses:
- **HTTP/REST** for request-response operations
- **Kafka** for event-driven communication between services
- **WebSocket** for real-time updates to clients

---

## 📦 ORDER FLOW

### 1. User Creates Order

```
┌─────────────────────────────────────────────────────────────────────┐
│ CLIENT (Next.js) - http://localhost:3000                            │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 1. HTTP POST /api/orders
                              │    Body: { items, shippingAddress }
                              │    Headers: { Authorization: Bearer <token> }
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API GATEWAY - http://localhost:3008                                 │
│ services/gateway/src/proxy/proxy.controller.ts                      │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 2. Proxy forwards to Order Service
                              │    POST http://localhost:3003/orders
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER SERVICE - http://localhost:3003                               │
│ services/order/src/presentation/controllers/order.controller.ts     │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 3. createOrder() handler
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ CREATE ORDER USE CASE                                               │
│ services/order/src/application/use-cases/create-order.usecase.ts    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ├─→ 4a. Save to MongoDB
                              │
                              └─→ 4b. Publish Kafka Event
                                     ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER EVENT PRODUCER                                                │
│ services/order/src/infrastructure/events/order.producer.ts          │
│                                                                      │
│ publishOrderCreated(order) {                                        │
│   topic: "order.created"                                            │
│   payload: {                                                        │
│     orderId, buyerId, items, total, status, createdAt              │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ KAFKA BROKER - localhost:9092                                       │
│ Topic: "order.created"                                              │
└─────────────────────────────────────────────────────────────────────┘
                              │
                 ┌────────────┼────────────┐
                 ↓            ↓            ↓
        ┌────────────┐ ┌────────────┐ ┌────────────┐
        │ INVENTORY  │ │  REALTIME  │ │  PAYMENT   │
        │  SERVICE   │ │  SERVICE   │ │  SERVICE   │
        └────────────┘ └────────────┘ └────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ REALTIME SERVICE - http://localhost:3009                            │
│ services/realtime/src/kafka/order-events.consumer.ts                │
│                                                                      │
│ handleOrderCreated(payload) {                                       │
│   // Emit to WebSocket clients                                      │
│   gateway.sendOrderCreated(userId, payload)                         │
│   gateway.sendOrderCreatedToAdmin(payload) // admin:order:created   │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket Emit
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ WEBSOCKET GATEWAY                                                   │
│ services/realtime/src/realtime/realtime.gateway.ts                  │
│                                                                      │
│ sendOrderCreated(userId, data) {                                    │
│   socket.to(`user:${userId}`).emit('order:created', data)          │
│ }                                                                   │
│                                                                      │
│ emitOrderCreatedToAdmin(data) {                                     │
│   socket.to('admin:dashboard').emit('admin:order:created', data)   │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                 ┌────────────┼────────────┐
                 ↓                         ↓
┌─────────────────────────┐   ┌─────────────────────────┐
│ USER CLIENT             │   │ ADMIN CLIENT            │
│ Socket.IO Connection    │   │ Socket.IO Connection    │
│                         │   │                         │
│ socket.on(              │   │ socket.on(              │
│   'order:created',      │   │   'admin:order:created',│
│   (data) => {           │   │   (data) => {           │
│     // Update UI        │   │     // Show new order   │
│     // Show toast       │   │     // Update stats     │
│   }                     │   │     // Show toast       │
│ )                       │   │   }                     │
│                         │   │ )                       │
└─────────────────────────┘   └─────────────────────────┘
```

---

### 2. Admin Updates Order Status

```
┌─────────────────────────────────────────────────────────────────────┐
│ ADMIN CLIENT - http://localhost:3000/admin/orders                   │
│ client/app/admin/orders/page.tsx                                    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 1. HTTP PATCH /api/orders/:id/status
                              │    Body: { status: "processing" }
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API GATEWAY - Proxy                                                 │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 2. Forward to Order Service
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER SERVICE                                                       │
│ services/order/src/application/use-cases/                           │
│   update-order-status.usecase.ts                                    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ├─→ 3a. Update MongoDB
                              │
                              └─→ 3b. Publish Kafka Event
                                     ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER EVENT PRODUCER                                                │
│                                                                      │
│ publishOrderUpdated(order) {                                        │
│   topic: "order.updated"                                            │
│   payload: {                                                        │
│     orderId, status, previousStatus, updatedAt                      │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ KAFKA - Topic: "order.updated"                                      │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ REALTIME SERVICE - Consumer                                         │
│                                                                      │
│ handleOrderUpdated(payload) {                                       │
│   gateway.sendOrderUpdated(userId, payload)                         │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket Emit
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ USER CLIENT                                                         │
│ client/app/orders/page.tsx                                          │
│                                                                      │
│ socket.on('order:updated', (data) => {                              │
│   // Update order status in UI (no refresh!)                        │
│   // Show toast: "Order status: processing"                         │
│ })                                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 3. User Cancels Order

```
┌─────────────────────────────────────────────────────────────────────┐
│ USER CLIENT - Click "Cancel Order"                                  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 1. HTTP POST /api/orders/:id/cancel
                              ↓
        API GATEWAY → ORDER SERVICE → Cancel Order Use Case
                              │
                              ├─→ 2a. Update MongoDB (status: cancelled)
                              │
                              └─→ 2b. Publish Kafka Event
                                     ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER PRODUCER                                                      │
│ topic: "order.cancelled"                                            │
│ payload: { orderId, buyerId, cancelledAt }                          │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓ Kafka
                              │
                 ┌────────────┼────────────┐
                 ↓                         ↓
       INVENTORY SERVICE            REALTIME SERVICE
       (restore stock)                     │
                                           ↓
                              WebSocket Emit to Admin
                                           │
                                           ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ADMIN CLIENT                                                        │
│                                                                      │
│ socket.on('admin:order:cancelled', (data) => {                      │
│   // Update order status to cancelled (no refresh!)                 │
│   // Show toast: "Order #XXX cancelled by customer"                 │
│   // Update stats                                                   │
│ })                                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🛍️ PRODUCT FLOW

### 1. Admin Creates Product

```
┌─────────────────────────────────────────────────────────────────────┐
│ ADMIN CLIENT - Add Product Form                                     │
│ client/app/admin/components/ProductForm.tsx                         │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 1. HTTP POST /api/products
                              │    Body: { name, description, price, sku, stock }
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ API GATEWAY                                                         │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 2. Forward to Product Service
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ PRODUCT SERVICE - http://localhost:3001                             │
│ services/product/src/presentation/controllers/product.controller.ts │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ 3. createProduct() handler
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ CREATE PRODUCT USE CASE                                             │
│ services/product/src/application/use-cases/create-product.usecase.ts│
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ├─→ 4a. Save to MongoDB
                              │
                              └─→ 4b. Publish Kafka Event
                                     ↓
┌─────────────────────────────────────────────────────────────────────┐
│ PRODUCT EVENT PRODUCER                                              │
│ services/product/src/infrastructure/events/product.producer.ts      │
│                                                                      │
│ publishProductCreated(product) {                                    │
│   topic: "product.created"                                          │
│   payload: {                                                        │
│     productId, name, sku, price, stock, category                    │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ KAFKA - Topic: "product.created"                                    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ REALTIME SERVICE - Consumer                                         │
│ services/realtime/src/kafka/inventory-events.consumer.ts            │
│                                                                      │
│ handleProductCreated(payload) {                                     │
│   gateway.broadcastToAdmins('admin:product:created', payload)      │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket Emit
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ ADMIN CLIENT                                                        │
│                                                                      │
│ socket.on('admin:product:created', (data) => {                      │
│   // Show notification: "New product added"                         │
│ })                                                                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 2. Inventory Update (Stock Changes)

```
┌─────────────────────────────────────────────────────────────────────┐
│ ORDER SERVICE - Order Created                                       │
│ (Stock needs to be deducted)                                        │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ Kafka Event: order.created
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ INVENTORY SERVICE - http://localhost:3002                           │
│ services/inventory/src/kafka/order-events.consumer.ts               │
│                                                                      │
│ handleOrderCreated(payload) {                                       │
│   // Deduct stock for each item                                     │
│   foreach(item in order.items) {                                    │
│     inventoryService.deductStock(item.productId, item.quantity)    │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ After stock update
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ INVENTORY EVENT PRODUCER                                            │
│ services/inventory/src/infrastructure/events/inventory.producer.ts  │
│                                                                      │
│ publishInventoryUpdated(inventory) {                                │
│   topic: "inventory.updated"                                        │
│   payload: {                                                        │
│     productId, stock, previousStock, sku                            │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ KAFKA - Topic: "inventory.updated"                                  │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────────┐
│ REALTIME SERVICE - Consumer                                         │
│                                                                      │
│ handleInventoryUpdated(payload) {                                   │
│   gateway.sendInventoryUpdate(payload) // to all clients            │
│   gateway.broadcastToAdmins('admin:inventory:updated', payload)    │
│                                                                      │
│   // Alert if low stock                                             │
│   if (payload.stock < 10) {                                         │
│     gateway.broadcastToAdmins('admin:alert', {                      │
│       type: 'inventory',                                            │
│       severity: 'warning',                                          │
│       message: 'Low stock alert'                                    │
│     })                                                              │
│   }                                                                 │
│ }                                                                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              │ WebSocket Emit
                 ┌────────────┼────────────┐
                 ↓                         ↓
┌─────────────────────────┐   ┌─────────────────────────┐
│ USER CLIENTS            │   │ ADMIN CLIENTS           │
│                         │   │                         │
│ socket.on(              │   │ socket.on(              │
│   'inventory:updated',  │   │   'admin:inventory:..', │
│   (data) => {           │   │   (data) => {           │
│     // Update product   │   │     // Update dashboard │
│     // Show "X left"    │   │     // Show low stock   │
│   }                     │   │     //   alerts         │
│ )                       │   │   }                     │
│                         │   │ )                       │
└─────────────────────────┘   └─────────────────────────┘
```

---

## 🔗 COMPLETE DATA FLOW SUMMARY

### Request-Response (HTTP via API Gateway)

```
Client → API Gateway → Service → Database
  ↑                                   │
  └───────── HTTP Response ←──────────┘
```

### Event-Driven (Kafka)

```
Service A → Kafka Topic → Service B, Service C, Service D
                              │
                              ↓
                     (Each processes independently)
```

### Real-Time Updates (WebSocket)

```
Service → Kafka → Realtime Service → WebSocket Gateway
                                            │
                  ┌─────────────────────────┼─────────────────────────┐
                  ↓                         ↓                         ↓
            User Client              Admin Client            Other Clients
```

---

## 📊 PORTS & SERVICES

| Service    | Port | Purpose                          |
|------------|------|----------------------------------|
| Client     | 3000 | Next.js frontend                 |
| Product    | 3001 | Product management               |
| Inventory  | 3002 | Stock management                 |
| Order      | 3003 | Order processing                 |
| User       | 3004 | User authentication              |
| Payment    | 3005 | Payment processing               |
| Gateway    | 3008 | API Gateway (reverse proxy)      |
| Realtime   | 3009 | WebSocket server + Kafka consumer|
| Kafka      | 9092 | Message broker                   |

---

## 🎯 KEY KAFKA TOPICS

| Topic              | Producer        | Consumers                    | Purpose                    |
|--------------------|-----------------|------------------------------|----------------------------|
| order.created      | Order Service   | Inventory, Realtime, Payment | New order placed           |
| order.updated      | Order Service   | Realtime                     | Status change              |
| order.cancelled    | Order Service   | Inventory, Realtime          | Order cancelled            |
| order.shipped      | Order Service   | Realtime                     | Order shipped              |
| order.delivered    | Order Service   | Realtime                     | Order delivered            |
| inventory.updated  | Inventory Svc   | Realtime                     | Stock level changed        |
| product.created    | Product Service | Realtime                     | New product added          |
| product.updated    | Product Service | Realtime                     | Product details changed    |

---

## 🔌 WEBSOCKET EVENTS

### Client → Server (Subscribe)
- `subscribe:orders` - Subscribe to order updates
- `subscribe:inventory` - Subscribe to inventory updates
- `subscribe:admin` - Subscribe to admin events (admin only)

### Server → User
- `order:created` - Order confirmation
- `order:updated` - Status changed
- `order:cancelled` - Order cancelled
- `inventory:updated` - Stock changed

### Server → Admin
- `admin:order:created` - New order from any user
- `admin:order:updated` - Order status changed
- `admin:order:cancelled` - Order cancelled by user
- `admin:inventory:updated` - Stock level changed
- `admin:product:created` - New product added
- `admin:product:updated` - Product details changed
- `admin:alert` - Critical alerts (low stock, etc.)

---

## 🚀 WHY THIS ARCHITECTURE?

### HTTP/REST (Request-Response)
- ✅ Simple CRUD operations
- ✅ Direct client-server communication
- ✅ Synchronous responses
- ❌ Not suitable for real-time updates

### Kafka (Event-Driven)
- ✅ Decouples services
- ✅ Scalable & reliable
- ✅ Event replay capability
- ✅ Multiple consumers per event
- ❌ Not suitable for client communication

### WebSocket (Real-Time)
- ✅ Bi-directional communication
- ✅ Low latency
- ✅ Push updates to clients
- ✅ No polling needed
- ❌ More complex than HTTP

---

**Combined:** HTTP for commands, Kafka for service communication, WebSocket for real-time UI updates! 🎉
